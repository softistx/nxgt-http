---
'@nxgt/openapi-httpyz': patch
---

Exports `PathMethods<Ops, Routes, Decoded>`, the client's `get`, `post`… for the methods the spec has an operation for, for a package that offers them on an object of its own.
