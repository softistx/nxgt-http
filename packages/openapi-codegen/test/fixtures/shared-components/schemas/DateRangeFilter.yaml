type: object
properties:
  from:
    type: string
    format: date-time
    description: Start date for the range filter
  to:
    type: string
    format: date-time
    description: End date for the range filter
