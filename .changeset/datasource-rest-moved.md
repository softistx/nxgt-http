---
'@nxgt/datasource-rest': patch
---

`@nxgt/datasource-rest` is now developed and published from `softistx/nxgt-http`, with its history. It no longer depends on `@nxgt/shared` or `lodash`, which it never imported.
