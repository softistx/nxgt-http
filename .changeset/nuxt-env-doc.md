---
'@nxgt/openapi-nuxt': patch
---

README: with `@nxgt/openapi-hono` 0.3.0 and a regenerated `hono.ts`, `c.env.event` is typed in the handlers of `createRoutes(createHonoApp())` too; the trap that said otherwise is gone.
