---
'@nxgt/openapi-httpyz': minor
---

`PathsOf<Routes, Method>` and `IdOf<Ops, Routes, Method, Path>` are exported: the paths that have an operation for a method, and the `operationId` of the operation at a route. They let a package built over the bound client type its calls the way the client does.
