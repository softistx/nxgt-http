# @nxgt/openapi-codegen

Generates TypeScript types and Zod 4 validators from an OpenAPI 3.1 or 3.2
document, split across files or not, and typed Hono routes bound to it.

**Not published yet.** This package is being built slice by slice and is
`private` until its first release; this README is written in full then.
